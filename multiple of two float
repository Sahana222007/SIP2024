#include <stdio.h>
void main()
{
    float a,b;
    float m;
    printf("Enter two float number : ");
    scanf("%f %f",&a,&b);
    m=a*b;
    printf("\nThe multiple of given two numbers are %f ",m);
    
}
